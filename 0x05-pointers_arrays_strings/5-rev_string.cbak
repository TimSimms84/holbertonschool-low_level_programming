#include "main.h"

/**
 * rev_string - Entry point
 *
 * @s: Point to the string
 *
 *
 *
 *
 * Return: void
 */

void rev_string(char *s)
{

	int i, j;
	char r[100];

	i = j = 0;
	while (s[i] != '\0')
	{
		r[i] = s[i];
		i++;
	}
	i--;
	while (i >= 0)
	{
		s[i] = r[j];
		i--;
		j++;
	}
}
